int motorPin = 14;

void setup() 

{ 
  pinMode(motorPin, OUTPUT);
  Serial.begin(9600);
} 
void loop() 
{ 
  for(int i=0 ; i<255 ; i+=10){
    analogWrite(motorPin, i);
    Serial.print(i);
    delay(1000);
  }
  analogWrite(motorPin, 0);
} 
